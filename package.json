{
  "private": true,
  "scripts": {
    "prod": "gulp --production",
    "dev": "gulp watch",
    "admin-lte": "npm install -g bower && cd ./public && bower install admin-lte",
    "laravel-db": "composer require yajra/laravel-datatables-oracle:^6.29.3 && php artisan vendor:publish --tag=datatables"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "laravel-elixir": "^5.0.0",
    "bootstrap-sass": "^3.0.0"
  }
}